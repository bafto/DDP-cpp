die Funktion Update() macht:
    schreibeZeile("Update").

die Funktion Male() macht:
    für jede Zahl x von 20 bis 50, mache:
        für jede Zahl y von 20 bis 50, mache:
            MalePixel([x; y], [255; 0; 0]).